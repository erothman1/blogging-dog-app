{{!-- Show single blogpost with title, content, creator username, date created, and existing comments --}}
{{!-- existing comments show content, creator username, and date created --}}
{{!-- have option to leave a comment  --}}
{{log this}}
<h1>{{title}}, Posted by @{{user.username}} on {{formatDate createdAt}}</h1>
<h2>{{content}}</h2>

{{#if comments.length}}
  <h3>Comments:</h3>
  {{#each comments}}
    <div>
      <p>{{this.content}}</p>
      <p>Posted by {{this.user.username}} on {{formatDate this.createdAt}}</p>
    </div>
  {{/each}}
{{else}}
  <p>No comments yet.</p>
{{/if}}